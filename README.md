# :car: Vehicle Sales

On Client folder...
```shell
cd client
```

Run the command:
```shell
npm start
```

Then open [http://localhost:3000](http://localhost:3000) to view it in your browser.

On Server folder...

```shell
cd server
```

Run the command:
```shell
npm run dev start
```